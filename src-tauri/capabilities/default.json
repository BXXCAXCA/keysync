{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main KeySync AI window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
